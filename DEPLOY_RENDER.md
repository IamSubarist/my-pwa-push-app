# Инструкция по деплою бэкенда на Render.com

## Быстрый старт

### Шаг 1: Подготовка VAPID ключей

Если у вас еще нет VAPID ключей, сгенерируйте их:

```bash
cd backend
python generate_vapid_keys.py
```

Сохраните сгенерированные ключи - они понадобятся на шаге 3.

### Шаг 2: Создание Web Service на Render

1. Войдите в [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш репозиторий (GitHub/GitLab/Bitbucket)
4. Выберите репозиторий с проектом

### Шаг 3: Настройка сервиса

Заполните следующие поля:

- **Name**: `pwa-push-backend` (или любое другое имя)
- **Region**: Выберите ближайший регион
- **Branch**: `main` (или ваша основная ветка)
- **Root Directory**: `backend` ⚠️ **ВАЖНО!**
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### Шаг 4: Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

#### 1. VAPID_PRIVATE_KEY

```
-----BEGIN PRIVATE KEY-----
(вставьте ваш приватный ключ полностью)
-----END PRIVATE KEY-----
```

**Важно**: При копировании многострочного ключа убедитесь, что переносы строк сохранены.

#### 2. VAPID_PUBLIC_KEY

```
(вставьте ваш публичный ключ в base64 формате, БЕЗ переносов строк)
```

#### 3. VAPID_EMAIL

```
mailto:your-email@example.com
```

#### 4. SUPABASE_URL (опционально)

```
https://your-project.supabase.co
```

#### 5. SUPABASE_KEY (опционально)

```
your-anon-key-here
```

### Шаг 5: Создание и деплой

1. Нажмите **"Create Web Service"**
2. Render начнет процесс деплоя (2-5 минут)
3. После успешного деплоя вы получите URL вида: `https://pwa-push-backend.onrender.com`

## Настройка фронтенда

### Если фронтенд уже на Render:

1. Перейдите в настройки вашего фронтенд сервиса
2. В разделе **"Environment Variables"** добавьте/обновите:
   ```
   VITE_API_URL=https://pwa-push-backend.onrender.com
   ```
   (замените на ваш реальный URL бэкенда)
3. Пересоберите фронтенд (Render сделает это автоматически при следующем деплое)

### Если фронтенд на другом хостинге:

1. Обновите `.env` файл:
   ```
   VITE_API_URL=https://pwa-push-backend.onrender.com
   ```
2. Пересоберите проект:
   ```bash
   npm run build
   ```
3. Загрузите новую сборку

## Проверка работы

### Проверка бэкенда:

1. Откройте `https://your-backend-url.onrender.com` - должен вернуть:

   ```json
   { "message": "PWA Push Notifications API", "status": "running" }
   ```

2. Откройте `https://your-backend-url.onrender.com/docs` - должна открыться документация Swagger

3. Откройте `https://your-backend-url.onrender.com/api/vapid-public-key` - должен вернуть публичный ключ

### Проверка фронтенда:

1. Откройте ваше PWA приложение
2. Откройте DevTools → Console
3. Проверьте, что нет ошибок подключения к API
4. Попробуйте подписаться на уведомления
5. Отправьте тестовое уведомление

## Важные моменты

### ⚠️ Free Tier ограничения

На бесплатном плане Render:

- Сервис "засыпает" после 15 минут неактивности
- Первый запрос после простоя может занять до 30 секунд (cold start)
- Для продакшена рекомендуется платный план

### 🔒 HTTPS

Render автоматически предоставляет HTTPS для всех сервисов - это необходимо для push-уведомлений.

### 📝 Многострочные переменные окружения

Для `VAPID_PRIVATE_KEY` в Render:

1. Скопируйте весь ключ включая `-----BEGIN PRIVATE KEY-----` и `-----END PRIVATE KEY-----`
2. Вставьте в поле переменной окружения
3. Убедитесь, что переносы строк сохранены

**Альтернативный способ через Render CLI:**

```bash
# Установите Render CLI
npm install -g render-cli

# Авторизуйтесь
render login

# Установите переменную из файла
render env:set VAPID_PRIVATE_KEY "$(cat path/to/private-key.pem)" --service your-service-name
```

## Решение проблем

### ❌ Бэкенд не запускается

1. Проверьте логи в Render Dashboard → Logs
2. Убедитесь, что **Root Directory** установлен в `backend`
3. Проверьте, что `requirements.txt` содержит все зависимости
4. Убедитесь, что **Start Command** правильный: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### ❌ Ошибка при подключении к бэкенду

1. Проверьте, что URL бэкенда правильный в переменных окружения фронтенда
2. Убедитесь, что бэкенд не "спит" (на free tier может быть задержка)
3. Проверьте CORS настройки (должны быть `allow_origins=["*"]` для начала)
4. Проверьте логи бэкенда на Render

### ❌ VAPID ключи не работают

1. Убедитесь, что ключи правильно скопированы в переменные окружения
2. Проверьте формат публичного ключа (должен быть base64 без переносов)
3. Проверьте логи бэкенда на наличие ошибок
4. Убедитесь, что приватный ключ содержит все строки включая BEGIN/END

### ❌ Push-уведомления не работают

1. Убедитесь, что фронтенд доступен по HTTPS
2. Проверьте, что Service Worker зарегистрирован
3. Проверьте разрешения на уведомления в браузере
4. Проверьте консоль браузера на наличие ошибок
5. Убедитесь, что бэкенд доступен и не спит

## Автоматический деплой

Render автоматически деплоит при каждом push в основную ветку репозитория. Это можно отключить в настройках сервиса.

## Мониторинг

В Render Dashboard вы можете:

- Просматривать логи в реальном времени
- Видеть метрики использования (CPU, память)
- Настраивать алерты

## Дополнительные настройки

### Health Check

Render автоматически проверяет здоровье сервиса через endpoint `/`. Убедитесь, что он возвращает успешный ответ.

### CORS для продакшена

Для продакшена рекомендуется ограничить CORS конкретными доменами:

```python
# В backend/main.py измените:
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-frontend-domain.com",
        "https://your-frontend-domain.onrender.com"
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## Чеклист деплоя

- [ ] VAPID ключи сгенерированы
- [ ] Web Service создан на Render
- [ ] Root Directory установлен в `backend`
- [ ] Build Command: `pip install -r requirements.txt`
- [ ] Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- [ ] Все переменные окружения добавлены
- [ ] Бэкенд успешно задеплоен
- [ ] URL бэкенда обновлен во фронтенде
- [ ] Фронтенд пересобран с новым URL
- [ ] Тестирование push-уведомлений прошло успешно

## Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Python Guide](https://render.com/docs/deploy-python)
- [Render Environment Variables](https://render.com/docs/environment-variables)
